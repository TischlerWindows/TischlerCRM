# Railway Production Environment Variables
# Set these in the Railway dashboard for each service

# === Frontend (Next.js) ===
# Public API endpoint (Railway will assign this)
NEXT_PUBLIC_API_URL=https://your-api-service.railway.app

# === Backend (Fastify API) ===
# Database Connection (automatically provided by Railway PostgreSQL plugin)
DATABASE_URL=postgresql://user:password@host:port/database

# JWT Secret (generate a random string)
JWT_SECRET=your-secure-random-jwt-secret-here

# Node environment
NODE_ENV=production

# === PostgreSQL (provided by Railway plugin) ===
# PGHOST, PGPORT, PGDATABASE, PGUSER, PGPASSWORD automatically set

# === Application Configuration ===
PORT=4000
API_PORT=4000
FRONTEND_PORT=3000
