/**
 * Re-export shim — the implementation has moved to ./formatting/rules-dialog.tsx.
 * This file is kept so that existing imports (`from '../formatting-rules-dialog'`)
 * continue to work without changes.
 */
export { FormattingRulesDialog } from './formatting';
