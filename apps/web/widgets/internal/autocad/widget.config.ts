import type { WidgetManifest } from '@/lib/widgets/types'

export const config: WidgetManifest = {
  id: 'autocad',
  name: 'AutoCad',
  description: 'Screw schedule items for this Project — inline-editable grid',
  icon: 'Wrench',
  category: 'internal',
  integration: null,
  defaultDisplayMode: 'full',
  configSchema: [],
}
